#pubspec.yaml
name: flutter_paralelo_1
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  http: ^0.13.6
  image_picker: ^1.0.4
  connectivity_plus: ^5.0.2
  wifi_iot: ^0.3.19+2
  geolocator: ^11.0.0
  flutter_blue_plus: ^1.32.4
  flutter_reactive_ble: ^5.0.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  generate: true


